$kind: http-request
name: 'Pemilik Subscription'
method: GET
url: '{{base_url}}/pemilik/paket'
order: 7000
headers:
  - key: Accept
    value: 'text/html'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Subscription page loads", function() {
          pm.expect(pm.response.code).to.be.oneOf([200, 302]);
      });
