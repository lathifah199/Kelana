$kind: collection
name: '🔐 Authentication'
description: |-
  Authentication endpoints for all user roles.
  Run the appropriate Login request first to get a session cookie and CSRF token.
  The collection pre-request script will automatically extract and set the CSRF token.
