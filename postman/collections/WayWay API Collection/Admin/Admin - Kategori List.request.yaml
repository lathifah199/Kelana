$kind: http-request
name: 'Admin - Kategori List'
method: GET
url: '{{base_url}}/admin/kategori'
order: 10000
headers:
  - key: Accept
    value: 'text/html'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Kategori list loads", function() {
          pm.expect(pm.response.code).to.be.oneOf([200, 302]);
      });
