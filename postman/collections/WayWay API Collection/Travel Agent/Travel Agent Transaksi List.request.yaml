$kind: http-request
name: 'Travel Agent Transaksi List'
method: GET
url: '{{base_url}}/travel-agent/subscriptions'
order: 10000
headers:
  - key: Accept
    value: 'text/html'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Transaksi list loads", function() {
          pm.expect(pm.response.code).to.be.oneOf([200, 302, 403]);
      });
